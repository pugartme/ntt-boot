name: Deploy Token API

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, token]
    env:
      KUBECONFIG: /etc/rancher/k3s/k3s.yaml

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image tag
        id: vars
        run: echo "TAG=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build Quarkus app
        working-directory: token
        run: ./mvnw clean package -DskipTests

      - name: Build image
        working-directory: token
        run: podman build --no-cache -t localhost/token-api:${TAG} .

      - name: Save image
        run: podman save localhost/token-api:${TAG} -o token-api.tar

      - name: Import into k3s
        run: |
          if [ -S /run/k3s/containerd/containerd.sock ]; then
            if id -nG | grep -qw "root"; then
              k3s ctr images import token-api.tar
            elif [ -w /run/k3s/containerd/containerd.sock ]; then
              k3s ctr images import token-api.tar
            else
              echo "Permission denied accessing /run/k3s/containerd/containerd.sock."
              echo "Make sure the self-hosted runner user can access the k3s containerd socket or run this step with sudo."
              exit 1
            fi
          else
            echo "k3s containerd socket not found at /run/k3s/containerd/containerd.sock"
            exit 1
          fi

      - name: Ensure namespace exists
        run: kubectl create namespace cqrs-lab --dry-run=client -o yaml | kubectl apply -f -

      - name: Apply Kubernetes manifests
        run: kubectl apply -f k8s/ -n cqrs-lab

      - name: Update deployment image
        run: |
          kubectl set image deployment/token-api \
          token-api=localhost/token-api:${TAG} -n cqrs-lab

      - name: Restart rollout
        run: kubectl rollout status deployment/token-api -n cqrs-lab
